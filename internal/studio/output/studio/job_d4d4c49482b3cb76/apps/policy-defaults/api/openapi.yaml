openapi: 3.1.0
info:
  title: Policy Defaults API
  version: 0.1.0
paths:
  /v1/policy-defaults/health:
    get:
      responses:
        '200':
          description: OK
  /v1/policy-defaults/workflows/execute:
    post:
      responses:
        '200':
          description: Workflow execution result
